{
  "name": "equipo-4-mlops",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "dev": "tsx src/server.ts",
    "test": "npm run test:tdd && npm run test:gherkin",
    "test:tdd": "tsx --test tests/tdd/app.test.ts",
    "test:gherkin": "tsx tests/gherkin/run-gherkin.ts",
    "check": "biome check .",
    "format": "biome format --write .",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx --env-file=.env src/data/migrate.ts",
    "db:seed": "tsx --env-file=.env src/data/seed.ts",
    "up": "docker compose up -d && npm run db:migrate && npm run db:seed && npm run dev"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.45.2",
    "express": "^4.19.2",
    "minio": "^8.0.7",
    "mysql2": "^3.9.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.31.10",
    "ts-node": "^10.9.2",
    "tsx": "^4.23.13",
    "typescript": "^5.0.0"
  }
}
